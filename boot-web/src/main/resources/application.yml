logging:
  file: logs/boot-web.log
  level:
    root: info
    com.dsjk.boot.web: debug

server:
  port: 7777

spring:
  application:
    name: boot-web
  aop:
    proxy-target-class: true

# dubbo
dubbo:
  application-name: web
  annotation-package: com.dsjk.boot.web.user
  protocol-name: dubbo
  protocol-port: 20882
  registry-protocol: zookeeper
  registry-address: zookeeper://127.0.0.1:2181?register=false
